% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Create_data.R
\name{Create_data}
\alias{Create_data}
\title{Create and clean episode data}
\usage{
Create_data(input_file, col_mapping, cleaned_path = NULL)
}
\arguments{
\item{input_file}{Path to the input CSV.}

\item{col_mapping}{Named character vector or list mapping standard names to user column names.
Example: list(patient_id="Patient_id", ICD="diag_code", start_date="date_start", end_date="date_end", episode_id="episode_id")}

\item{cleaned_path}{Path to write the cleaned CSV. If NULL, a default path in `tempdir()` is used.}
}
\value{
The `cleaned_path` (character string). Also invisibly returns the cleaned data.frame.
}
\description{
Reads a CSV file, renames columns using a mapping, parses dates, cleans ICD codes,
and writes a cleaned CSV to `cleaned_path`.
}
